apiVersion: v1
kind: Service
metadata:
  name: {{ include "openaire-corpus.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "openaire-corpus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openaire-corpus.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: app
