{
  "name": "bnf-broker",
  "private": true,
  "type": "module",
  "description": "BnF partner-API broker — OAuth token + shared rate governance + verbatim proxy.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts"
  },
  "dependencies": {
    "undici": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
