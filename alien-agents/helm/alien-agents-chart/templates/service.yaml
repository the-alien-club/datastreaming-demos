apiVersion: v1
kind: Service
metadata:
  name: {{ include "alien-agents.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "alien-agents.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alien-agents.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: app
