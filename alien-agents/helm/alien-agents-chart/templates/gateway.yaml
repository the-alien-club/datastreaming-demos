{{- if .Values.istio.gateway.create }}
{{- $ownHostsRaw := list }}
{{- range .Values.istio.hosts }}
  {{- if eq (.gateway | default "shared") "own" }}
    {{- $ownHostsRaw = append $ownHostsRaw .host }}
  {{- end }}
{{- end }}
{{- $ownHosts := $ownHostsRaw | uniq }}
{{- if $ownHosts }}
---
apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: {{ include "alien-agents.fullname" . }}-gateway
  namespace: {{ .Values.istio.gatewayNamespace }}
  labels:
    {{- include "alien-agents.labels" . | nindent 4 }}
    app.kubernetes.io/component: gateway
spec:
  selector:
    istio: ingressgateway
  servers:
    # HTTP server for ACME HTTP-01 challenges (no redirect).
    - port:
        number: 80
        name: http-alien-agents
        protocol: HTTP
      hosts:
        {{- range $ownHosts }}
        - {{ . }}
        {{- end }}
      tls:
        httpsRedirect: false

    # HTTPS server with cert-manager issued certificate.
    # The credential covers every `own` host as SANs (see certificate.yaml).
    - port:
        number: 443
        name: https-alien-agents
        protocol: HTTPS
      hosts:
        {{- range $ownHosts }}
        - {{ . }}
        {{- end }}
      tls:
        mode: SIMPLE
        credentialName: {{ include "alien-agents.fullname" . }}-tls
{{- end }}
{{- end }}
