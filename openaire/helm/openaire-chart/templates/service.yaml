apiVersion: v1
kind: Service
metadata:
  name: {{ include "openaire.fullname" . }}
  labels:
    {{- include "openaire.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openaire.selectorLabels" . | nindent 4 }}
